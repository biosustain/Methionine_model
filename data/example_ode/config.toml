name = "test-ode"
kinetic_model_file = "kinetic_model.toml"
priors_file = "priors.toml"
experiments_file = "experiments.toml"
likelihood = true

[cmdstanpy_config]
iter_warmup = 5
iter_sampling = 5
chains = 4
refresh = 1

[ode_solver_config]
max_num_steps = 1e9

[laplace_options]
draws=500
